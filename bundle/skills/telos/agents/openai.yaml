interface:
  display_name: "Telos SDD"
  short_description: "Orchestrate intent-to-code integrity"
  default_prompt: "Use $telos to take this repository change from intent through verified implementation."
policy:
  allow_implicit_invocation: true
