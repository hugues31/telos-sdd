[package]
name = "telos-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
toml = "0.9"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
